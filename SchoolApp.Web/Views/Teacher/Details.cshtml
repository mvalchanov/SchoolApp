@*@model User
@{
    ViewData["Title"] = $"{Model.FullName}Details";
}

<table class="table table-hover text-center">
    <thead>
        <tr>
            <th>Id</th>
            <th>First Name</th>
            <th>Middle Name</th>
            <th>Last Name</th>
            <th>Groups</th>
            <th>Options</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Model.UserId</td>
            <td>@Model.FirstName</td>
            <td>@Model.MidName</td>
            <td>@Model.LastName</td>
            <td>
                @foreach (var group in Model.Groups)
                {
                    @group.Group.Name <br />
                }
            </td>
        </tr>
    </tbody>
</table>

@foreach (var group in Model.Groups)
{
    <div class="card text-white bg-primary mb-3" style="max-width: 20rem;">
        <div class="card-header">Group:</div>
        <div class="card-body">
            <h4 class="card-title">@group.Group.Name</h4>
            <p class="card-text">Students in group: @group.Group.Users.Where(s => s.User.IsTeacher == false).Count()</p>
        </div>
    </div>
}*@

